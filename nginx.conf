server {
    listen 80;
    server_name split.api.spl-unboxed.com _;

    client_max_body_size 25m;

    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;

    location / {
        proxy_pass         http://api:8000;
        proxy_http_version 1.1;

        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_set_header   Connection        '';
        proxy_buffering    off;
        proxy_cache        off;

    }
}

# TODO: once DNS for split.api.spl-unboxed.com points at this instance,
# run certbot for this domain and restore the HTTPS/443 server block
# (see git history for the previous nexus-api.linkworks.in version).
